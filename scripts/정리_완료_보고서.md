# Scripts 폴더 정리 완료 보고서

## 작업 완료 내역

### ✅ 완료된 작업

1. **폴더 구조 생성**
   - `main/` - 메인 프로덕션 스크립트
   - `generate/` - 리포트/패키지 생성
   - `extract/` - 데이터 추출
   - `utils/` - 유틸리티
   - `special/` - 특수 기능
   - `docs/` - 문서
   - `archive/` - 구버전 보관
     - `archive/old_build/` - 구버전 빌드 스크립트
     - `archive/patches/` - 구버전 패치 (patch1, patch2, patch3)
     - `archive/verification/` - 검증 스크립트
     - `archive/output/` - 구버전 Excel 출력 파일
     - `archive/data/` - 대용량 파일

2. **파일 이동 완료**
   - ✅ 구버전 빌드 스크립트 → `archive/old_build/`
   - ✅ 구버전 패치 스크립트 → `archive/patches/`
   - ✅ 검증 스크립트 → `archive/verification/`
   - ✅ Excel 출력 파일 → `archive/output/`

3. **경로 수정 완료**
   - ✅ `generate_mammoet_package.py`: v4 파일 경로로 업데이트
   - ✅ `generate_submission_package.py`: v4 파일 경로로 업데이트
   - ✅ `update_stage_values.py`: v4 파일 경로로 업데이트

4. **README.md 작성 완료**
   - 스크립트 사용 가이드 작성
   - 폴더 구조 설명
   - 주요 사용 방법 안내

### ⚠️ 수동 이동 필요

일부 파일이 열려있거나 다른 프로세스가 사용 중이어서 자동 이동이 실패했습니다. 다음 파일들을 수동으로 이동해주세요:

**메인 스크립트:**
- `build_bushra_v4_standalone.py` → `main/`
- `bushra_operations.py` → `main/`

**생성 스크립트:**
- `generate_mammoet_package.py` → `generate/`
- `generate_mammoet_submission.py` → `generate/`
- `generate_submission_package.py` → `generate/`
- `generate_vessel_sketch.py` → `generate/`

**추출 스크립트:**
- `extract_gateab_tide_data.py` → `extract/`
- `extract_stage_data_from_pdf.py` → `extract/`

**유틸리티:**
- `PATCH1106.py` → `utils/`
- `update_stage_values.py` → `utils/`

**문서:**
- `함수.patch` → `docs/`

## 최종 폴더 구조

```
scripts/
├── main/                    # 메인 프로덕션 스크립트
│   ├── build_bushra_gateab_v4_hybrid.py
│   ├── build_bushra_v4_standalone.py (수동 이동 필요)
│   └── bushra_operations.py (수동 이동 필요)
│
├── generate/                # 리포트/패키지 생성
│   ├── generate_height_report_pdf.py
│   ├── generate_vessel_sketch.py (수동 이동 필요)
│   ├── generate_mammoet_package.py (수동 이동 필요)
│   ├── generate_submission_package.py (수동 이동 필요)
│   └── generate_mammoet_submission.py (수동 이동 필요)
│
├── extract/                 # 데이터 추출
│   ├── extract_vessel_depth_from_pdf.py
│   ├── extract_stage_data_from_pdf.py (수동 이동 필요)
│   └── extract_gateab_tide_data.py (수동 이동 필요)
│
├── utils/                   # 유틸리티
│   ├── pdf_to_excel_converter.py
│   ├── update_stage_values.py (수동 이동 필요)
│   └── PATCH1106.py (수동 이동 필요)
│
├── special/                 # 특수 기능
│   └── patch4.py
│
├── docs/                    # 문서
│   ├── coordinate_reference.md
│   └── 함수.patch (수동 이동 필요)
│
├── archive/                 # 구버전 보관
│   ├── old_build/
│   │   ├── build_bushra_package.py ✅
│   │   └── build_bushra_package_hourly_fix.py ✅
│   ├── patches/
│   │   ├── patch1.py ✅
│   │   ├── patch2.py ✅
│   │   └── patch3.py ✅
│   ├── verification/
│   │   └── verify_*.py ✅
│   └── output/
│       └── (구버전 Excel 파일들) ✅
│
└── README.md                # 스크립트 사용 가이드 ✅
```

## 다음 단계

1. 모든 Python 스크립트 파일을 닫기
2. 위의 "수동 이동 필요" 파일들을 해당 폴더로 이동
3. 스크립트 실행 테스트
4. 필요시 import 경로 수정

## 참고사항

- 모든 경로 수정은 완료되었습니다
- README.md에 사용 방법이 상세히 기록되어 있습니다
- 구버전 파일들은 모두 archive 폴더에 보관되어 있습니다

